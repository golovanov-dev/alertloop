// AlertLoop admin runtime configuration.
// apiBase: base URL of the AlertLoop API. Empty string = same origin, which is
// how the console is served (at /admin by the AlertLoop binary). Only change
// this if you host the built assets separately from the API.
window.__ALERTLOOP__ = { apiBase: "" };
